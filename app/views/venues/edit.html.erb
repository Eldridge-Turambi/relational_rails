<%= form_with url: "/venues/#{@venue.id}", method: :patch, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :lights %>
  <%= f.text_field :lights %>

  <%= f.label :capacity %>
  <%= f.text_field :capacity %>

  <%= f.submit "Update This Venue" %>
<% end %>
